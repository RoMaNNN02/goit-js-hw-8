<ul>{{#each this}}
    <li data-id="{{id}}" class="bookmarkItem">
      <a href="{{link}}" class="bookmarkLink">{{link}}</a>
      <button class="removeBtn">Видалити</button>
    </li>
  {{/each}}
</ul>